replicaCount: 1

image:
  repository: nexlabgitopsdemo.azurecr.io
  name: nexlab-gitops-app-backend
  tag: develop
  pullPolicy: IfNotPresent

containerPort: 80

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
  path: /api
  hosts: []
  tls: []
